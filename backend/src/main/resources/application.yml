spring:
  h2:
    console:
      enabled: true
  # 종서의 테스트를 위한 코드
#  datasource:
#    url: jdbc:h2:mem:testdb
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false


logging:
  level:
    org.hibernate.SQL: debug

